1.终端启动MySQL
$ /etc/init.d/mysql start(stop, restart)

2.登录MySQL
$ mysql -h hostname -u root -p (用root账户登录), 然后输入密码

3.查看所有的数据库名字
mysql> show databases;

4.选择一个数据库操作
mysql> use database_name;

5.查看当前数据库下所有的表名
mysql> show tables;

6.创建一个数据库
mysql> create database database_name;

7.删除一个数据库
mysql> drop database database_name;

8.创建一个表
mysql> create table mytest( uid bigint(20) not null, uname varchar(20) not null);

9.删除一个表
mysql> drop table mytest;

10.设置创建的数据库的编码
mysql> ALTER DATABASE <db-name> DEFAULT CHARACTER SET utf8 COLLATE utf8_unicode_ci;

11.查看数据库编码
mysql> show variables like 'character%';

12.查看数据库信息
mysql> \s

13.备份与还原
$mysqldump -h hostname -u username -p password databasename > backupfile.sql
$mysqldump -h hostname -u username -p password databasename | gzip > backupfile.sql.gz
还原MySQL数据库的命令
$mysql -h hostname -u username -p password databasename < backupfile.sql
$gunzip < backupfile.sql.gz | mysql -u username -p password databasename
